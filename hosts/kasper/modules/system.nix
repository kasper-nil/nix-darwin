{ ... }:
{
  system = {
    # Backwards-compat marker. Read `darwin-rebuild changelog` before bumping.
    stateVersion = 6;

    # User that user-scoped settings (homebrew, system.defaults) apply to.
    primaryUser = "kasper";
  };
}
